spring.datasource.url=${POSTGRES_DB:jdbc:postgresql://127.0.0.1:5432/somedb}
spring.datasource.username=${POSTGRES_USER:someuser}
spring.datasource.password=${POSTGRES_PASSWORD:somepassword}
spring.datasource.driver-class-name=org.postgresql.Driver
server.port=8090
server.servlet.context-path=/api
spring.flyway.clean-disabled=false
spring.flyway.clean-on-validation-error=false
spring.flyway.locations=classpath:db/migration
spring.flyway.table=schema_history
spring.mvc.pathmatch.matching-strategy=ANT_PATH_MATCHER