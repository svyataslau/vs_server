# The Challenge app server

### To start local development:

1) create a .env file in the project root using .env.template and apply it in "Edit configuration".
2) run docker-compose to initialize db
